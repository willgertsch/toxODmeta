 # test_that(
#   "logistic model fits", {
#
#     d = data.frame(
#       x = c(1, 2, 3, 4, 5),
#       y = c(0.2, 0.2, 0.4, 0.5, 0.7)
#     )
#
#     mod = fit_nonlinear_model(d$y, d$x, "logistic")
#     expect_equal(is.numeric(mod$theta), T)
#   }
# )
